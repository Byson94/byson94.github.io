import{F as e,G as t,K as n,P as r,W as i,m as a,x as o,y as s}from"./WWTSD2Qb.js";import"./xihTtKlq.js";var c=n({default:()=>m,metadata:()=>l}),l={title:`Writing The Ideal Wayland Compositor`,date:`2026-07-18T00:00:00.000Z`,description:`Writing the wayland compositor of my dreams.`},{title:u,date:d,description:f}=l,p=o(`<p>In the last post, I talked about what I think is the ideal wayland compositor. And this post will talk about
I decided to build it myself, the progress I made, and the limitation I am facing.</p> <h2>Project</h2> <p>Let me introduce you to the project first. I called it <strong>buzzay</strong>. And it works exactly as the window manager
I mentioned in the previous blog post of mine (but with a catch which I’ll get to).</p> <p>It is plugin first, and loads plugins that are inside the following directories:</p> <ul><li><code>/usr/lib/buzzay-plugins/</code></li> <li><code>~/.local/share/buzzay-plugins/</code></li></ul> <p>They can be loaded, and messaged to, like so:</p> <pre class="language-bash"></pre> <p>For those interested in the project, here are the links:</p> <ul><li>Source: <a href="https://github.com/Byson94/buzzay" rel="nofollow">https://github.com/Byson94/buzzay</a></li> <li>Website: <a href="https://byson94.is-a.dev/buzzay" rel="nofollow">https://byson94.is-a.dev/buzzay</a></li></ul> <h2>Progress</h2> <p>Its only been a few days since I started writing it and I believe I made a fair amount of progress. The compositor
can be loaded and can display cursor and windows too.</p> <p>As of right now, it implements the <code>xdg-shell</code>, <code>xdg-decoration</code>, <code>viewporter</code>, <code>cursor-shape</code>, <code>gamma</code>, <code>idle-notify</code>,
and <code>idle-inhibit</code> protocols, along with a partial <code>layer-shell</code> implementaiton.</p> <p>I am now working on the window management system and on finishing the <code>layer-shell</code> implementation.</p> <h2>Limitations</h2> <p>Ehh, I was quickly proven why compositors don’t just follow this architecture.
Here are the main limitations and issues that I faced.</p> <h3>Safety</h3> <p>Exposing the compositor server to the plugins is a safety risk. A plugin can maliciously or accidentally corrupt
or pass invalid data to the server which will result in the whole compositor crashing. And the server is something
that gets updated quite often. If a plugin relies on it, then it would have to contantly be recompiled to keep up
to date with the compositor. If it is not recompiled and is out of date, then it will simply crash the whole compositor
which is <strong>a terrible user experience</strong>.</p> <h3>Complexity of wlroots</h3> <p><code>wlroots</code> is severely undocumented and complex. Trying to map it 1:1 will make the compositor complex too, which
I am actively trying to avoid. And I cant pass the raw wlroots data around because wlroots API is unstable.
I cant expose the compositor server and let the plugins figure it out on their own too because of the safety
reasons mentioned above.</p> <h3>Lack of room to grow</h3> <p>If I don’t expose the compositor server to the plugins, then everyting would have to run through hooks.
This isn’t ideal because not every case can be covered through hooks. If I did expose the compositor server
to the plugins then I will suffer from the safety issues mentioned right above!</p> <p>Let me give you a more precise example. Take a window manager like niri for example. When you
try to move a window through inputs (using something like <code>Super+Click</code>), you would see that the compositor
would highlight where the window will stick to when you leave it. But this thing isn’t really easy
to implement in buzzay just because of the sole reason that it should leave the work to plugins.</p> <p>This is an issue exclusive to this architecture. It is neither possible to cover every scenario nor possible
to let the plugins have full access to the compositor server while ensuring fully safety. Trying to implement
a safe API that covers most scenarios is time consuming and has a chance of making the compositor end up
like X11.</p> <h2>Solutions & Vision</h2> <p>Although I am still to find a cleaner solution, I decided to make the plugin API an abstraction that allows plugins to hook into existing
compositor functions. Although this severely reduces the things a plugin can do, it is a necessary sacrifice I am forced to make.
The compositor <strong>has to be opinionated</strong>. Plugins no longer can just do “anything”. I will now be building the plugin API to be more of a
configuration system rather than an extension system. This is the only way in which both the compositor and the plugin can coexist safely.</p>`,1);function m(n){var o=p(),c=e(r(o),12);a(c,()=>`<code class="language-bash">$ buzzay <span class="token parameter variable">--load</span> <span class="token operator">&lt;</span>name<span class="token operator">></span>
$ buzzay <span class="token parameter variable">--msg</span> <span class="token operator">&lt;</span>name<span class="token operator">></span> <span class="token operator">&lt;</span>arg<span class="token operator"><span class="token file-descriptor important">1</span>></span> <span class="token operator">&lt;</span>arg<span class="token operator"><span class="token file-descriptor important">2</span>></span> <span class="token punctuation">..</span>.</code>`,!0),t(c),i(36),s(n,o)}export{c as t};